SPACE
        format
                bitfield
                        byte
                        byteval
        layout
                register

REGISTER SET
        layout
        getter
        setter

bitfield definition:
        -> regformat bitfields -> format decode -> various decoders
        -> space bitfield byte
        -> space bitfield
        -> self bytevals
           -> self, by name
           -> self, by value

namespace unification:
        regset#
        regsetmap#
        reg#
        regformat#
        bitfield#
        bitfield-byte#
        reglayout#

bit access:
        bitfield-byte   -> bitmask
        register-set    -> accessor
        register        -> selector